<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.hr.expense</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="hr_expense.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//div[@name='expenses_setting_container']" position="inside">
                <div class="col-12 col-lg-6 o_setting_box" id="expense_policy_settings">
                    <div class="o_setting_left_pane" />
                    <div class="o_setting_right_pane">
                        <span class="o_form_label">Expense Cancellation Policy</span>
                        <div class="text-muted">
                            Set the cancellation policy to standard by default.
                            Standard odoo will change state expense to 'Refused',
                            you can configure following for change state your policy.
                        </div>
                        <div class="content-group">
                            <div class="row mt16">
                                <label
                                    string="Payment"
                                    for="expense_payment_cancel"
                                    class="col-lg-3 o_light_label"
                                />
                                <field name="expense_payment_cancel" />
                                <span class="col-lg-12 text-muted">
                                    when you cancel payment that depend on expense.
                                    you can configure 'Payment' that back state to Posted or Refused (Standard Odoo)
                                </span>
                            </div>
                            <div class="row">
                                <label
                                    string="Move"
                                    for="expense_move_cancel"
                                    class="col-lg-3 o_light_label"
                                />
                                <field name="expense_move_cancel" />
                                <span class="col-lg-12 text-muted">
                                    when you cancel move that depend on expense.
                                    BUT you can configure 'Move' that back state to Submit, Approve or Refused (Standard Odoo)
                                </span>
                            </div>
                        </div>
                    </div>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
